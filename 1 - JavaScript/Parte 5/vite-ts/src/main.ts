let nombre: string;
nombre = "juan";

console.log(nombre);

const a: number[] = [];
a.push(23);


function suma(n1: number, n2: number): number {
    if(n1 > 0)
        return n1 + n2;
    return 0;
}
